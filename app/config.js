// ============================================================
// config.js  —  Step 1: API configuration
//
// Change API_BASE_URL to your deployed backend URL when going
// to production. Every fetch call imports from here so you
// only ever need to update one line.
// ============================================================

const CONFIG = {
  // Local development — change to your Railway/Render URL for production
  API_BASE_URL: "http://localhost:5000",
};
